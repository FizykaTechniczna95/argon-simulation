################ KOMORKA ####################
# komorka symulacyjna o boku
N
int(2)

# liczba atomow
M
int(2*const['N']**3)

# bok k. elementarnej
A
2/float(const['M'])
################## STAŁE #####################
# Liczba Avogadro
N_a
6.022045e23

# Stała Boltzmana [J/K]
K_b
1.38066e-23
################# PARAMETRY ###################
# Masa molowa 'm_u' [u]
m_u
39.948

# Masa molowa 'm' [kg]
m
const['m_u']/(const['N_a']*1e3)

# Gęstość 'rho' [kg/m3]
rho
1394

# Teperatura T [K]
T
87.3

# Temperatura odniesiena eps_k [K]
eps_k
119.8

# Energia Odniesienia eps_j [J]	
eps_j
const['eps_k'] * const['K_b'] 

# Krok czasowy dt [s]
dt
1.0e-14

# Kroki dochodzenia do równowagi 'step_r' 
step_r
1000

# Kroki symulacji 'step_s'
step_s
int(10000)
######### Wielkości zredukowane i inne ###########
# Długosc boku kostki 'L' [m]
L
( const['N'] * const['m'] / const['rho'] )**1/3

# H
H
const['L']/2.

# dt_r
dt_r
const['dt'] * ( const['eps_j'] / (const['m'] * const['H']**2 ) )**0.5

# dt_r2
dt_r2
const['dt_r']**2

# Temperatura zredukowana T_r 
T_r
const['T'] * const['K_b'] / const['eps_j']

# Predkosc maksymalna 'v_rmax'
v_rmax
const['dt_r'] * ( 3 * const['T_r'] )**0.5 

# 'sigma' 
sigma
3.41e-10

# Sigma zredukowana 'sigma_r'
sigma_r
const['sigma'] / const['H']

# Sigma 2 'sigma_r2'
sigma_r2
const['sigma_r']**2 
